#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: number to revise
 * Return: value of absolute (success)
 */

int _abs(int n)
{
	int i = -1;
	int j;

	if (n <= 0)
	{
		j = n * i;
		return (j);
	}

	else
	{
		return (n);
	}
}
